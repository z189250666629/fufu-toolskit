{
  "name": "network-detect",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "go run .",
    "start": "go run .",
    "build": "go build -o network-detect ."
  }
}
